# 应用名称
spring.application.name=community
# 应用服务 WEB 访问端口  server指的是ServerProperties类
server.port=8080
server.servlet.context-path=/community
# 数据库驱动：
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 数据源名称
spring.datasource.name=defaultDataSource
# 数据库连接地址
spring.datasource.url=jdbc:mysql://localhost:3306/ls?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
# 数据库用户名&密码：
spring.datasource.username=root
spring.datasource.password=ls525857,
# 程序申请连接时，进行连接有效性检查
spring.datasource.druid.test-on-borrow=true
# 失效连接主要通过testWhileIdle保证，若获取到不可用的数据库连接，由应用处理异常
spring.datasource.druid.test-while-idle=true


# THYMELEAF (ThymeleafAutoConfiguration)
# 开启模板缓存（默认值： true ） thymeleaf指的是ThymeleafProperties类
spring.thymeleaf.cache=true
# 检查模板是否存在，然后再呈现
spring.thymeleaf.check-template=true
# 检查模板位置是否正确（默认值 :true ）
spring.thymeleaf.check-template-location=true
#Content-Type 的值（默认值： text/html ）
spring.thymeleaf.content-type=text/html
# 开启 MVC Thymeleaf 视图解析（默认值： true ）
spring.thymeleaf.enabled=true
# 模板编码
spring.thymeleaf.encoding=UTF-8
# 要被排除在解析之外的视图名称列表，⽤逗号分隔
spring.thymeleaf.excluded-view-names=
# 要运⽤于模板之上的模板模式。另⻅ StandardTemplate-ModeHandlers( 默认值： HTML5)
spring.thymeleaf.mode=HTML5
# 在构建 URL 时添加到视图名称前的前缀（默认值： classpath:/templates/ ）
spring.thymeleaf.prefix=classpath:/templates/
# 在构建 URL 时添加到视图名称后的后缀（默认值： .html ）
spring.thymeleaf.suffix=.html

# Mybatis Properties
mybatis.mapper-locations=classpath:mapper/*.xml
# 在***mapper.xml中的resultType,parameterType中可以省去全限定类名
mybatis.type-aliases-package=com.nowcoder.community.entity  
# 支持自动生成主键的数据库
mybatis.configuration.useGeneratedKeys=true
# 将带有下划线的表字段映射为驼峰格式的实体类属性
mybatis.configuration.mapUnderscoreToCamelCase=true

# logger
logging.level.com.nowcoder.community=debug
logging.file.path=/Users/liusheng126.com/Desktop/ls/log/communityLog

# MailProperties邮件的配置,smtp服务器,客户端的设置
spring.mail.host=smtp.sina.cn
# 端口的默认名字，基本都是465,
spring.mail.port=465 
spring.mail.username=ls525857@sina.cn
spring.mail.password=305ba5471adf8392
spring.mail.protocol=smtps
#spring.mail.properties.mail.smtp.ssl.enable=true
#spring.mail.properties.mail.smtl.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# communtity
community.path.domain=http://localhost:8080

